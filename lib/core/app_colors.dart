import 'package:flutter/material.dart';

/// Centralized color definitions for the app
/// Use like: `AppColors.primary`, `AppColors.white`, etc.
class AppColors {
  // Dark background
  static const Color darkBackground = Color(0xFF1F1F39);

  // White background
  static const Color white = Color(0xFFFFFFFF);

  // Grey shades
  static const Color grey = Color(0xFF858597);
  static const Color lightGrey = Color(0xFFB8B8D2);
  static const Color offWhite = Color(0xFFF0F0F2);

  // Primary color (Blue tone)
  static const Color primary = Color(0xFF3D5CFF);

  // Google brand colors
  static const Color googleGreen = Color(0xFF34A853);
  static const Color googleBlue = Color(0xFF4285F4);
  static const Color googleRed = Color(0xFFEA4335);
  static const Color googleYellow = Color(0xFFFBBC05);
}
